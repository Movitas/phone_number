require 'rake'

desc "Downloads and parses country metadata from Google's libphonenumber project"
task :update do
  puts "Update placeholder"
end

namespace :update do
  desc "Downloads XML file from Google"
  task :download do
    puts "Download placeholder"
  end
  
  desc "Parses XML into YAML files"
  task :parse do
    puts "Parse placeholder"
  end
end